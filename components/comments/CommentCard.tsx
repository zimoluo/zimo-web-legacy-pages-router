import { ThemeType } from "@/interfaces/themeMaps";

interface Props {
    commentData: any;
    theme: ThemeType;
}

const CommentUser: React.FC<Props> = ({ theme }) => {
    return (
        <>
        </>
    );
}