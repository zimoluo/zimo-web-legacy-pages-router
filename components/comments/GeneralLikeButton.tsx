import { ThemeType } from "@/interfaces/themeMaps";

interface Props {
  theme: ThemeType;
}

const GeneralLikeButton: React.FC<Props> = ({ theme }) => {
  return <div></div>;
};

export default GeneralLikeButton;
